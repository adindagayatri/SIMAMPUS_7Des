<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simampus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIMAMPUS">
        <activity android:name=".AddEditPinjam"></activity>
        <activity android:name=".AddEditMember" />
        <activity
            android:name=".Member"
            android:label="@string/title_activity_member"
            android:theme="@style/Theme.SIMAMPUS.NoActionBar" />
        <activity
            android:name=".Pinjam"
            android:label="@string/title_activity_pinjam"
            android:theme="@style/Theme.SIMAMPUS.NoActionBar">
        </activity>
        <activity android:name=".AddActivity" />
        <activity
            android:name=".Home"
            android:label="Buku"
            android:theme="@style/Theme.SIMAMPUS.NoActionBar" />
        <activity
            android:name=".Buku"
            android:label="Buku1"
            android:theme="@style/Theme.SIMAMPUS.NoActionBar" />
        <activity android:name=".LoginAdmin" />
        <activity android:name=".RegisterAdmin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>